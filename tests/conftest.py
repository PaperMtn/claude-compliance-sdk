"""Shared pytest fixtures.

The full suite of HTTP fixtures (canned responses, mock transports,
real-API integration helpers) lands in Phase 2 alongside the transport
implementation. This file exists now so the test runner can pick up
``conftest`` discovery from the start.
"""
